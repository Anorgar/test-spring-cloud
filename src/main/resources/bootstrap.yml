micronaut:
  application:
    name: order-resilience-consumer
  config-client:
    enabled: true

spring:
  cloud:
    config:
      enabled: true
      #uri: http://api-recette-config.cultura.intra:80/order-resilience-consumer
      uri: http://config:8000/
